pull_request_rules:
  - name: Automatic merge on approval
    conditions:
        - or:
          - "author=dependabot"
          - "author=StephanAkkerman"
          - "author=TimKoornstra"
    actions:
      merge:
        method: merge
